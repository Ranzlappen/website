---
layout: null
# [Status field update] Custom sitemap replacing jekyll-sitemap plugin.
# Only includes posts where status == "published" (draft/unpublished/
# placeholder posts are noindexed test content — see _includes/head.html).
# Pages with a file extension in their URL (404.html, search.json,
# sitemap.xml, llms.txt, …) are utility output, not content — skipped.
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd"
        xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for page in site.pages %}{% if page.url and page.sitemap != false %}{% unless page.url contains "." %}
  <url>
    <loc>{{ page.url | absolute_url | xml_escape }}</loc>
    {% if page.date %}<lastmod>{{ page.date | date_to_xmlschema }}</lastmod>{% endif %}
    {% if page.url == "/" %}<changefreq>weekly</changefreq><priority>1.0</priority>
    {% elsif page.url == "/blog/" %}<changefreq>weekly</changefreq><priority>0.9</priority>
    {% elsif page.url == "/categories/" or page.url == "/tags/" %}<changefreq>weekly</changefreq><priority>0.6</priority>
    {% elsif page.url == "/about/" or page.url == "/contact/" %}<changefreq>monthly</changefreq><priority>0.5</priority>
    {% else %}<changefreq>yearly</changefreq><priority>0.3</priority>
    {% endif %}
  </url>
  {% endunless %}{% endif %}{% endfor %}
  {% for post in site.posts %}{% if post.status == "published" %}
  <url>
    <loc>{{ post.url | absolute_url | xml_escape }}</loc>
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>
  {% endif %}{% endfor %}
</urlset>
