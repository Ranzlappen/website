/**
 * Scheduled function: runs every 5 minutes to archive expired pending topic requests.
 */
export declare const cleanupExpiredRequests: import("firebase-functions/v2/scheduler").ScheduleFunction;
